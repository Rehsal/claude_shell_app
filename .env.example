# Optional configuration for Claude Shell App
# Copy to .env and modify as needed

# Directory for storing project data (default: ./data)
APP_DATA_DIR=./data

# Anthropic API key (not required for basic shell functionality)
ANTHROPIC_API_KEY=your_api_key_here
